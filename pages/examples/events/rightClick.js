/***************************************************************

鼠标右键事件

注意：使用该方法后，默认的右键方法将被取消掉

***************************************************************/

canvas.oncontextmenu = function(e){
    e.preventDefault();
    
    // 把下面的语句替换成自己的代码
    circle(Mouse.x, Mouse.y, 10); 
};