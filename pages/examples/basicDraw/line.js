/***************************************************************

line(x1, y1, x2, y2, *lineWidth, *color)

在画布上画出一条线段

* 起点为 (x1, y1)
* 终点为 (x2, y2)
* 线的粗细为lineWidth，可选参数，默认为1
* 颜色为color，可选参数，默认为 "orange"


***************************************************************/
canvas.showAxis();

line(100, 100, 300, 150);

line(100, 200, 300, 250, 3);

line(100, 300, 300, 350, 5, 'red');