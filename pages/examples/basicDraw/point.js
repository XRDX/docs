/***************************************************************


point(x, y, *color)

在画布上画出一个点：

* 点的位置为（x,y）
* 颜色为为color，可选参数，默认为 "orange"


***************************************************************/
canvas.scale(20, 20);
canvas.showAxis();

point(10, 10);

point(20, 20, 'green');

point(30, 10, 'red');
